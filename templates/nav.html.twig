{% extends 'base.html.twig' %}

{% block body %}

    <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Traitement CDR</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">

                    {% if is_active is defined and  is_active == "home" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('home') }}">
                                Tableau de bord
                            </a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('home') }}">Tableau de bord</a>
                        </li>
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('unwanted_number') }}">Numeros indesirables</a>
                    </li>

                    {% if is_active is defined and  is_active == "matrice_communication" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('matrice_communication') }}">Matrice des communications</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('matrice_communication') }}">Matrice des communications</a>
                        </li>
                    {% endif %}

                    {% if is_active is defined and  is_active == "matrice_contact_communs" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('matrice_contact_communs') }}">Contacts en commun</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('matrice_contact_communs') }}">Contacts en commun</a>
                        </li>
                    {% endif %}

                    {% if is_active is defined and  is_active == "communication_shema" %}
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('schema-commun') }}">Schema des communications</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href={{ path('schema-commun') }}>Schema des communications</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('home_wizard') }}">Assistant de configuration</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">

        {% block content %}



        {% endblock %}

    </div>




{% endblock %}